"use strict";
// import fs from 'fs';
Object.defineProperty(exports, "__esModule", { value: true });
// export async function alteraIntervaloTempo({
//     Tempo
// }: {
//     Tempo: number;
// }) {
//     // converte para milissegundos
//     config.interval = Tempo * 60 * 1000; 
//     // 3. Salvar de volta no arquivo
//     fs.writeFileSync(configPath, JSON.stringify(config, null, 2));
//     console.log('Interalo de desconexao aumentado!');
// }
// export async function pausarBot({
//     Tempo
// }: {
//     Tempo: number;
// }){
//     const agora = new Date();
//     const timestamp = agora.getTime();
//     config.tempo_pausa = (Tempo * 60 * 1000) + timestamp; 
//     // 3. Salvar de volta no arquivo
//     fs.writeFileSync(configPath, JSON.stringify(config, null, 2));
//     console.log(`Bot pausado por ${config.tempo_pausa / 60 / 1000} minutos!`);
// }
// export async function pararBot(){
//     config.bot_parado = true;
//     // 3. Salvar de volta no arquivo
//     fs.writeFileSync(configPath, JSON.stringify(config, null, 2));
//     console.log('Bot parado!');
// }
// export async function iniciarBot(){
//     config.bot_parado = false;
//     // 3. Salvar de volta no arquivo
//     fs.writeFileSync(configPath, JSON.stringify(config, null, 2));
//     console.log('Bot iniciado!');
// }
